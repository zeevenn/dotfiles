# Local SSH configuration (machine-specific)
# Copy to ~/.ssh/config.local and customize
#
# This file is NOT tracked by git - safe for machine-specific settings

# Example: Custom server
# Host myserver
#   HostName server.example.com
#   User username
#   Port 22
#   IdentityFile ~/.ssh/id_rsa_myserver
