# npm configuration
# Public settings only - sensitive data goes in ~/.npmrc.local

# Global package path
prefix=$HOME/.npm-global

# Use faster registry (optional)
# registry=https://registry.npmmirror.com

# Increase timeout
fetch-timeout=60000

# Progress bar
progress=true

# Include ~/.npmrc.local if it exists (for auth tokens, etc.)
# Note: npm doesn't support includes, so this is just a reminder
# Actual sensitive config should be in ~/.npmrc.local
